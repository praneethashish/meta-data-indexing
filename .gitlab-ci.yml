stages:
  - lint
  - test
  - build

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  UV_CACHE_DIR: "$CI_PROJECT_DIR/.cache/uv"

cache:
  paths:
    - .cache/pip
    - .cache/uv
    - .venv/

before_script:
  - curl -LsSf https://astral.sh/uv/install.sh | sh
  - export PATH="$HOME/.local/bin:$PATH"
  - uv venv
  - source .venv/bin/activate
  - uv pip install -e .

lint:
  stage: lint
  image: python:3.10
  script:
    - ruff check . # Covers linting and dead code
    - ruff format --check .
    - mypy bookextractor/

test:
  stage: test
  image: python:3.10
  script:
    - pytest tests/ --cov=bookextractor --cov-report=term --cov-report=xml
  coverage: '/TOTAL.*\s+(\d+\.?\d*)%/'
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml

docker-build:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  before_script: [] # Skip uv install for docker build
  script:
    - docker build .
  only:
    - main
